<h1>Enter a new story: </h1>

<%= simple_form_for(@story) do |f| %>

	<%= f.input :title %>
	<%= f.input :link %>
	<%= f.input :upvotes, collection: 0..100 %>
	<%= f.input :category %>

	<%= f.submit "Add" %>

<% end %>

<%= link_to "Back Home", stories_path %>